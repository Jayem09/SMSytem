# --- DATABASE CONFIGURATION ---
DB_ROOT_PASSWORD=your_secure_root_password
DB_NAME=smsystem_prod
DB_USER=sms_user
DB_PASSWORD=your_secure_user_password
DB_PORT=3306

# --- BACKEND CONFIGURATION ---
JWT_SECRET=your_super_secret_jwt_key
PORT=8080

# --- VPS CONFIGURATION ---
VPS_PUBLIC_IP=168.144.46.137
# Add branch IPs to the firewall as needed:
# sudo ufw allow from [BRANCH_IP] to any port 3306
